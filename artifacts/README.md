# Artifact policy

Generated traces, evaluation tables, plots, model adapters, and reports are written under this directory during experiments and ignored by default.

An artifact may be promoted into version control only when it is small, redistributable, connected to a reproducible command, and required to support a published claim. Model weights and private traces remain outside Git.
